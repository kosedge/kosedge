# Data has moved

Pipeline data now lives under **`apps/web/data/`**. See **[../../data/README.md](../../data/README.md)** for layout, run order, and paths.

All scripts use `pipeline_paths.py` (app root); run from `apps/web`.
