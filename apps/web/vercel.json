{
  "framework": "nextjs",
  "regions": ["iad1"],
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "buildCommand": "pnpm run build",
  "crons": [
    {
      "path": "/api/cron/warm-page-data",
      "schedule": "* * * * *"
    }
  ]
}
