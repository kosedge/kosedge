# Deprecated path

Work items now live in `../queue/runtime/` (gitignored).

`camp-flag-*.json` day dumps must not land in PRs — generate with
`python scripts/nfl/queue_camp_sot_flags.py --queue` after merge.
