# CFB research efficiency → scoring (as_of 20260915)

Research-only. **`production_promote=false`.** Boards stay Coming soon.

Protocol locked before knobs: `docs/cfb/EVAL_PROTOCOL.md` (`6aae7b7a`).

| File | Role |
| --- | --- |
| `epa_clean_baselines.json` | IBF-v1 re-report of #560 2025 holdout (same obs keys) |
| `holdout_2025_obs_keys.tsv` | SHA `009ce549…` (match) |
| `scoring_validation_report.json` | Train / 2023 val / 2024 W≥10 confirm / 2025 dev-ev |
| `scoring_summary.json` | One-page call |
| `artifact_checksums.json` | SHA-256 |

**Call: revise** (confirmation margin beats baselines; total does not).  
Shrunk HFA used for scoring: **5.87 points** (not `0.244 × plays`).

See `docs/cfb/CFB_RESEARCH_EFF_SCORING_2026-09-15.md`.
