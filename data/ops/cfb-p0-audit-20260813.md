# CFB P0 audit — 2026-08-13

**Date:** 2026-08-13  
**Branch:** `feat/cfb-p0-p1-warehouse-spine` → `deploy-vercel`  
**Engine (code):** `cfb-season-engine-v0.9-inseason` (`services/model-service/src/services/cfb_season_engine/priors.py`)  
**Doctrine:** Model = research fair (versioned) → KEI = late reprice → Edge = KEI vs market only. Zero future leakage. Immutable prediction snapshots. PASS > invented PLAY.

## Do not claim lock

**CFB is not locked.** Same enterprise bar as the NFL 2026 preseason lock; this sport has not cleared it. Do not pin a CFB engine. Do not publish KEI. Do not invent PLAY/LEAN. Do not copy Project Game onto Edge Board as edge.

Prod `GET /api/cfb/season-engine/status` on 2026-08-13 returned `{"engine_version":"","error":"Status failed (500)"}`. Railway public model-service URL was not curled successfully from this pass. Engine string below is from **code**, not a live Railway health probe.

---

## What is real

| Asset | Evidence |
| --- | --- |
| Warehouse v1 (2026-08-12) | Games + results 2020–2025: **5,196 FINAL**, all with kickoff. Close spread **4,994**. Open spread **4,184**. Lake snaps **174,606** (2020–2026). PBP 2014–2025: **1,819,153** plays / **10,297** games. FCS games **kept** (701). |
| Leakage contract | `strictly_before_kickoff`. Fallbacks: `available_at.date < game_date`, else `feature_week < game_week`. Unprovable → not available. Forbidden: same-season finals, end-of-year SOS, post-hoc recruiting, “what the freshman became”. Tests exist and hold. |
| Odds lake join | Close = last **owned** snap strictly before kickoff (not a true lock). Open = first snap. SDV is fill. 1,012 warehouse games unmatched to lake — rows kept, no silent year drop. |
| Walk-forward W0–4 | Early-season research fair does **not** beat close. W0–1: ATS **47.7%**, MAE **8.36** (n=439). Do not publish KEI from the current prior. |
| Scale/HFA holdout (research) | `scale=2.75`, HFA `3.8` cut holdout W0–4 MAE 9.60 → 6.90. ATS still a coin flip (48.0%). `used_in_spread: false`. Not wired into project-game / KEI / Edge. |
| Roster SoT | `packaged_espn_roster_2026`, `as_of=2026-08-12`, 134 teams, 13,353 athletes. CFBD skipped. TEX Arch Manning incumbent is real in the pack. |
| MODEL desks | `/pro/cfb/model` and `/pro/cfb/project-game` are labeled PRESEASON + MODEL (`cfb-truth-label.ts`). Honesty note: not a published handicap; Edge is books only; no KEI. |
| Edge Board CFB | `sportIsMarketsOnlyEdgeBoard("cfb")` is true. `resolveKeiGames("cfb")` returns `[]`. KEI columns stay blank. |
| Fair-lines / KEI pages | Stub: “not shipped.” Empty table. No `kei_lines_cfb.json` pipeline. |

Season close/open (games / close spread / open spread):

| Season | Games | Close | Open |
| ---: | ---: | ---: | ---: |
| 2020 | 571 | 512 | 477 |
| 2021 | 891 | 814 | 696 |
| 2022 | 900 | 838 | 717 |
| 2023 | 911 | 907 | 713 |
| 2024 | 965 | 965 | 793 |
| 2025 | 958 | 958 | 788 |

---

## What is approximate / theater

| Item | Honest read |
| --- | --- |
| Densified schedule | `packaged_sample_densified` — **not** the official 2026 FBS slate. Seed + synthetic fill. Season-sim `n_sims` HTTP default **15** (cap 200). Σ wins would be the densified game count (~780), never NFL 272. |
| Project-game fair | Still driven by packaged 2025 SP+ carry + roster overlay. PBP adj / 2026 prior attach as `research_prior` with `used_in_spread: false`. |
| Camp QB / σ | Pack heuristic = ESPN 2026 roster QB1 by 2025 attempts. UGA (Puglisi vs Stockton), MICH (Fowler-Nicolosi vs Underwood), FSU (DeNobile vs Daniels), LSU (Clark vs Leavitt) are labeled incumbent with σ≈4.3. Ops treats them as **open / high uncertainty**. Do not trust Week 0 σ. |
| Returning production / portal | Team-level class-year proxy, not measured SNAP%. Portal-out incomplete. PSU/BAY class flags wrong (portal not tagged). |
| Conference membership | 2026 packaged map (`season=0`). Not a realignment history. |
| Lake close | Last owned snap ≠ lock when densify is sparse (UGA–CLEM 2024: close captured Aug 27 for Aug 31 kickoff). |
| Efficiency `available_at` | Bucket max; often unusable (PBP↔ESPN id mismatch). Leakage proof is `feature_week < game_week`. |
| Hist-cal / performance tracking | Approximate reconstruction + paper log. Not a conserved futures/wins product. |
| Prod status | Web CFB status probe **500** on 2026-08-13. Desk may render “unreachable” even when code is `v0.9-inseason`. |
| Fair Lines nav | Exists as a shell. Copy says CFB fair-lines are not connected. That is honest empty, not a handicap. |
| CFP / natty | **No product.** No playoff/national-championship desk, no conserved futures table. |
| Release gate / pin | **None.** NFL has `preseason_release_gate.py` + lock tag. CFB has neither. |

This is **not** theater in the warehouse or the leakage tests. It **is** theater if anyone reads Project Game, densified season paths, or the research prior as a published handicap.

---

## Surfaces (load? empty? markets-only? fake KEI?)

| Surface | State | Fake KEI? |
| --- | --- | --- |
| `/pro/cfb/model` | MODEL desk. Loads if model-service is up; prod status was **500** this audit. Truth badges PRESEASON + MODEL through 2026-08-28. | No |
| `/pro/cfb/project-game` | MODEL research card. Same badges. Spread/WP are research, not KEI. | No |
| `/edge-board/cfb` | **Markets-only.** Books Open/Best. KEI / Edge / Tag blank. | No |
| `/pro/cfb/fair-lines` | Shared stub. `resolveKeiGames("cfb")` → `[]`. Pending note: not shipped. | No |
| `/pro/kei-lines/cfb` | Empty. Label “KEI (not shipped)”. | No |
| Season wins / CFP / natty | Missing. Densified sim is not a futures product. | n/a |

---

## Leakage risks (still live)

1. Using 2026 roster/QB pack on 2020–25 walk-forward (future overlay). Current harness does **not** do this — keep it that way.
2. Treating lake close as lock.
3. Feeding same-season finals / end-of-year SOS / post-hoc recruiting into Week 0–N.
4. Trusting efficiency `available_at` timestamps instead of `feature_week < game_week`.
5. Mutating a research snapshot after injury news instead of inserting a new `(model_version, as_of, game_id)` row. **P1 spine exists to stop this.**
6. Joining Project Game to Edge Board without a real KEI. That would be invented edge.

---

## Gaps vs a Week 0 bar (NFL-class)

Week 0 / first FBS slate: **Saturday 2026-08-29**. Preseason cutoff in product: **2026-08-28**.

| Bar | CFB now |
| --- | --- |
| Honest empty > fake KEI | **Yes** |
| Truth labels on MODEL desks | **Yes** (shipped 2026-08-12) |
| Official 2026 FBS schedule | **No** — densified sample |
| Fresh, named-starter SoT (camp QB overrides) | **No** — four high-profile misses |
| Research fair beats close on holdout (ATS + MAE) | **No** — scale helped MAE; ATS still ~50% |
| Immutable prediction store | **No** until P1 of this PR (DDL + parquet/JSONL writer) |
| Backtest-run registry | **No** until P1 |
| KEI vs market tags | **No** — correctly absent |
| Conserved season wins / CFP | **No** |
| Release gate + pin | **No** |
| `n_sims` enterprise depth | **No** — default 15 |
| Postgres warehouse loaded in prod | **No** — DDL only; HD parquet is SoT |

CFB can be used as a **MODEL + markets** preview. It is not a Week 0 betting desk.

---

## Ranked gaps for P2+

1. **Camp QB SoT overrides** (UGA / MICH / FSU / LSU, plus class-flag fixes PSU/BAY) before any prior is trusted for Week 0 σ. News → expert note → pack override → prior recompute. Do not invent names absent from ESPN.
2. **Official 2026 FBS slate** replacing `packaged_sample_densified`. Densified ≠ product schedule.
3. **Preseason prior redesign (P2)** — program + roster + QB + uncertainty with `available_at`, era weights, FCS-separated strength. Current prior is a ranking prior, not a closer. Holdout scale/HFA is research only (`used_in_spread: false`).
4. **Walk-forward bar** after (1)+(3): MAE/RMSE vs close, ATS, totals ATS, Brier/log-loss, CLV when open+close exist. Thin slices flagged. No in-sample retune on the same file.
5. **Immutable prediction + backtest_runs usage** (P1 schema) — write research fairs; never UPDATE; injury = new row.
6. **Conference membership by season** + historical coaches/rosters materialized (stubs in P1 DDL only).
7. **Opponent-adj EPA mart** as a registered feature (PBP + week snapshots exist; not a full mart product).
8. **Odds-lake unmatched 1,012** — name/FCS join, not silent drop.
9. **Release gate + pin** before any CFB KEI or engine bump. PASS > invented PLAY.
10. **KEI only after** a trustworthy pure fair exists. Edge Board stays markets-only until then.
11. CFP / natty / conserved futures — after official slate + real season sim depth. Not this month’s theater.
12. Postgres operational slices only — **no** multi-decade bulk odds reload into Railway.

---

## Honesty (sticky)

- MODEL desks exist. Edge is markets-only. **No fake KEI.**
- Densified schedule ≠ official FBS slate.
- `n_sims` default is **15**.
- No CFP / natty product.
- No CFB release gate / pin.
- Do not claim lock. Do not bump `cfb-season-engine-v0.9-inseason` in this work. Do not redesign the prior here (P2). Do not mix NFL model invention.

## P1 of this PR (holes only)

Immutable `cfb_wh_model_predictions` + `cfb_wh_backtest_runs` (+ coach / team-season stubs), HD JSON/JSONL/parquet writer that rejects overwrite, coverage report, walk-forward dry-run. No Edge Board KEI. No engine bump. No prior rewrite.
