# NFL Props eligibility — 2026-08-12

**Decision:** Primary Props board is an **investable** universe (skill positions + involvement floors), not a roster export. OL / DL / LB / DB / ST and kicker anytime-TD rows with 0.0 model means are hard-dropped. Prefer missing rows over junk. No UI softmax. Config: `apps/web/lib/nfl-props-eligibility.ts` and `services/model-service/src/services/nfl_props_eligibility.py`.
