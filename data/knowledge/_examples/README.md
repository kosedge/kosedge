# EXAMPLE cards only

Files here use `status: EXAMPLE`. They are **format demos**, not institutional history.

- Do **not** cite them as prior grades.
- Do **not** copy their claim text into real assignments as if Kos Edge said it.
- Real cards live under `nfl/*` and `cfb/*` when filed.
